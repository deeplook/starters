[project]
name = "aws-lambda-demo"
version = "0.1.0"
description = "An example for a minimal AWS Lambda function."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.39.0",
]
