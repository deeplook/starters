```bash
# In your terminal
docker run -d -p 9000:8080 --env-file ./.env --name fastapi-basic-app fastapi-basic
```

TLDR

```shell
uv sync
cp .env.example .env
source .env && sudo lsof -i :$MYAPP_PORT | grep LISTEN
uv run main.py &
source .env && sudo lsof -i :$MYAPP_PORT | grep LISTEN
source .env && curl -X GET http://127.0.0.1:$MYAPP_PORT/items
uv run pytest
pkill -SIGSTOP -f -x "uv run main.py"

docker compose up -d
source .env && curl -X GET http://127.0.0.1:$MYAPP_PORT/items
docker compose down
```
